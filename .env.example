# Django settings
DJANGO_SECRET_KEY=django-insecure-change-me-in-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database settings
DB_NAME=bazary_dev
DB_USER=bazary_user
DB_PASSWORD=bazary_pass
DB_HOST=localhost
DB_PORT=5432

# JWT settings
JWT_SECRET_KEY=jwt-secret-key-change-me
JWT_ACCESS_TOKEN_LIFETIME=15
JWT_REFRESH_TOKEN_LIFETIME=7

# Email settings
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=apikey
EMAIL_HOST_PASSWORD=your_sendgrid_api_key_here
DEFAULT_FROM_EMAIL=noreply@bazary.com

# SendGrid API Key (same as EMAIL_HOST_PASSWORD when using SMTP)
SENDGRID_API_KEY=your_sendgrid_api_key_here

# Site settings for emails
SITE_NAME=Bazary
FRONTEND_URL=http://localhost:3000

# Redis settings
REDIS_URL=redis://localhost:6379/0

# AWS S3 settings (leave empty for development)
USE_S3=False
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=us-east-1

# Sentry (leave empty for development)
SENTRY_DSN=
ENVIRONMENT=development
